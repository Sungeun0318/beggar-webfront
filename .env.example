# 이 파일을 .env 로 복사해서 값을 채우세요 (cp .env.example .env)
# .env 는 git에 올라가지 않습니다. 실제 키/시크릿은 .env 에만 둡니다.

# Spring 백엔드 베이스 URL (로컬 기본값)
VITE_API_BASE_URL=http://127.0.0.1:8080

# 카카오 JavaScript 앱 키 (웹 카카오 로그인용). 백엔드 /auth/kakao 활성화 후 사용.
VITE_KAKAO_JS_KEY=
